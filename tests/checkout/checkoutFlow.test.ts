/**
 * Test stub for checkout flow.
 * TODO: wire up test runner (Jest / Vitest) and DOM test utilities.
 */
import { describe, it } from 'vitest'

describe('Checkout form', () => {
  it('should call /api/checkout on submit (stub)', async () => {
    // TODO: mount `CheckoutForm` with a mocked cart store and fetch mock
    // expect global.fetch to be called with '/api/checkout' and the proper body
  })
})
